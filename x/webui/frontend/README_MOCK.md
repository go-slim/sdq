# SDQ WebUI - Mock Development Environment

这是一个基于 Preact + Vite + Tailwind CSS 的 SDQ 队列监控 WebUI，使用 Vite 内置的 mock API 进行开发。

## 🚀 功能特性

### 📊 Dashboard 页面
- **队列概览统计** - 显示总任务数、各状态任务数、主题数量等
- **操作统计** - 显示 Puts、Reserves、Deletes 等操作次数和吞吐量
- **主题列表** - 显示所有主题的任务统计信息，支持快速操作
- **自动刷新** - 可配置的实时数据更新（1s-30s 间隔）

### 📁 Topic 详情页面
- **主题统计信息** - 显示该主题的详细任务分布
- **任务列表** - 支持状态过滤（All/Ready/Delayed/Reserved/Buried）
- **分页浏览** - 每页 20 条记录，支持页码导航
- **批量操作** - 支持批量 Kick 或删除 Buried 任务
- **单个操作** - 支持对单个任务的 Kick 和删除操作

### 🔍 Job 详情页面
- **完整元数据** - 显示任务的所有状态信息和时间戳
- **统计信息** - 显示 Reserves、Timeouts、Releases 等统计
- **任务内容** - 格式化显示 JSON 格式的任务体
- **操作功能** - 支持 Kick 和删除任务（根据状态）

### 🎨 设计特点
- **响应式设计** - 适配桌面和移动设备
- **状态标识** - 不同颜色标识任务状态
- **交互友好** - 加载动画、错误处理、确认提示
- **导航清晰** - 面包屑导航，便于页面跳转

## 🛠️ 使用方法

### 启动开发服务器
```bash
npm run dev
```
访问 http://localhost:5174/ 即可查看应用。

### Mock API 端点

系统使用 Vite 内置的 mock API，完全模拟 SDQ 的真实数据结构：

#### 队列概览
- `GET /api/overview` - 返回队列整体统计信息
```json
{
  "TotalJobs": 1250,
  "ReadyJobs": 85,
  "ReservedJobs": 12,
  "DelayedJobs": 8,
  "BuriedJobs": 3,
  "Topics": 5,
  "TotalWaitingWorkers": 4,
  "Uptime": 216720,
  "Puts": 15420,
  "Reserves": 12850,
  "Throughput": {
    "PutsPerSecond": 3.2,
    "ReservesPerSecond": 2.8
  }
}
```

#### 主题管理
- `GET /api/topics` - 返回所有主题列表
- `GET /api/topics/{topic}` - 返回指定主题的统计信息
- `GET /api/topics/{topic}/jobs` - 返回主题下的任务列表
  - 支持查询参数：`?state=ready&page=1&page_size=20`

#### 任务管理
- `GET /api/jobs/{id}` - 返回任务详细信息
- `POST /api/jobs/{id}/kick` - 踢出任务
- `DELETE /api/jobs/{id}` - 删除任务

#### 批量操作
- `POST /api/topics/{topic}/kick` - 批量踢出 Buried 任务
- `POST /api/topics/{topic}/delete-buried` - 批量删除 Buried 任务

## 🎯 页面路由

- `/` - Dashboard 主页
- `/topics/{topic}` - Topic 详情页
- `/jobs/{id}` - Job 详情页

## 🔄 切换到真实后端

要连接到真实的 Go 后端，修改 `vite.config.js`：

```javascript
// 注释掉 mock 相关代码
// plugins: [preact()],

// 启用代理配置
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
    },
  },
}
```

## 📱 响应式支持

- **桌面端** (>768px) - 完整功能和布局
- **平板端** (768px 及以下) - 适配布局调整
- **手机端** (480px 及以下) - 移动优化布局

## 🎨 UI 组件

### 状态标识
- **Ready** - 绿色 (#d4edda)
- **Delayed** - 黄色 (#fff3cd)  
- **Reserved** - 蓝色 (#cce5ff)
- **Buried** - 红色 (#f8d7da)
- **Enqueued** - 灰色 (#e2e3e5)

### 交互组件
- **Loading Spinner** - 数据加载动画
- **Error Boundary** - 错误边界处理
- **Pagination** - 分页导航组件
- **Filter Buttons** - 状态过滤按钮
- **Confirmation Dialogs** - 操作确认提示

## 🚀 部署

```bash
# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

## 📊 技术栈

- **Frontend**: Preact 10.27.2
- **Router**: preact-iso 2.11.0 (原生 a 标签导航)
- **Build**: Vite 7.3.0
- **Styling**: Tailwind CSS 3.4.0 + PostCSS
- **Mocking**: Vite 内置中间件

这个 Mock 环境完全基于 SDQ 的真实数据结构设计，提供了接近生产环境的开发体验。